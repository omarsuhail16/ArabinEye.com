@model ByArabianEye.Models.Offer
@{
    ViewData["Title"] = "Create Offer";
}

<div class="container my-5">
    <h2 class="mb-4 text-center fw-bold">📝 إضافة عرض جديد | New Offer</h2>

    <form asp-action="Create" method="post" enctype="multipart/form-data">
        <!-- ✅ عرض الأخطاء -->
        <div asp-validation-summary="All" class="alert alert-danger"></div>

        <div class="mb-3">
            <label asp-for="Title" class="form-label">عنوان العرض / Offer Title</label>
            <input asp-for="Title" class="form-control" />
            <span asp-validation-for="Title" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Description" class="form-label">الوصف / Description</label>
            <textarea asp-for="Description" class="form-control" rows="3"></textarea>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Country" class="form-label">الدولة / Country</label>
            <select asp-for="Country" class="form-select">
                <option value="">-- Select Country --</option>
                <option>Azerbaijan</option>
                <option>Georgia</option>
                <option>Russia</option>
                <option>Albania</option>
                <option>Kazakhstan</option>
                <option>Uzbekistan</option>
            </select>
            <span asp-validation-for="Country" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="OfferType" class="form-label">نوع العرض / Offer Type</label>
            <select asp-for="OfferType" class="form-select">
                <option value="">-- Select Type --</option>
                <option>Hotel</option>
                <option>Tour</option>
                <option>Transport</option>
            </select>
            <span asp-validation-for="OfferType" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Days" class="form-label">عدد الأيام / Days</label>
            <input asp-for="Days" class="form-control" type="number" min="1" />
            <span asp-validation-for="Days" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Price" class="form-label">السعر / Price</label>
            <input asp-for="Price" class="form-control" type="number" step="0.01" />
            <span asp-validation-for="Price" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="ExpiryDate" class="form-label">تاريخ الانتهاء / Expiry Date</label>
            <input asp-for="ExpiryDate" class="form-control" type="date" />
            <span asp-validation-for="ExpiryDate" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label class="form-label">الصورة / Upload Image</label>
            <input type="file" name="ImageFile" class="form-control" />
        </div>

        <button type="submit" class="btn btn-success w-100">إضافة العرض | Submit Offer</button>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
